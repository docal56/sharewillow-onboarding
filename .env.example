# ShareWillow Onboarding Experiment
#
# No server-side environment variables are required.
# The Anthropic API key is entered by the user directly in the app
# and passed per-request to the API route. It is never stored.
